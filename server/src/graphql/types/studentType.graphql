type Meta {
  createdAt: String
  updatedAt: String
}

type Student {
  id: ID!
  name: String
  sex: String
  age: Int
  info: Info
  meta: Meta
}

type Query {
  students: [Student]
  student(id: ID!): Student
}
